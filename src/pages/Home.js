import React from 'react';
import { Typography } from '@mui/material';

function Home() {
    return (
        <div>
            <Typography variant="h4" gutterBottom>Welcome to BookApp</Typography>
            <Typography variant="body1">Manage your books efficiently.</Typography>
        </div>
    );
}

export default Home;
